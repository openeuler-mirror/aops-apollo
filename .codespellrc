[codespell]
skip = .git,*.lock,node_modules,.next,coverage,build,dist,vendor,third_party
# 项目术语/专有名词加入忽略列表；注意：该值留空会导致 codespell 报
# "argument -L/--ignore-words-list: expected one argument"，不填就整行删除。
# ignore-words-list = ba,teh

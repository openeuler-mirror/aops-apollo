[Unit]
Description=A-Ops apollo service
After=network.target

[Service]
Type=forking
ExecStart=/usr/bin/aops-cli service --name aops-apollo
ExecStop=/usr/bin/aops-cli service --name aops-apollo --stop
Restart=on-failure
RestartSec=1
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
